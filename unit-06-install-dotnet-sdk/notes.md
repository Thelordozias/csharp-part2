# Unit 6 – Install .NET SDK

Initial check:
- dotnet --version returned: No .NET SDKs were found

Installation:
- Installed .NET 10 SDK using VS Code / .NET install workflow

Verification:
- dotnet --version -> 10.0.102
- Restarted terminal / VS Code to ensure SDK was detected
